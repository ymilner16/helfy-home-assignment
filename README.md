# helfy-home-assignment
This is a home assignment for Junior SRE role at Helfy.

An end-to-end event driven architecture featuring a Node.js backend, database persistence and Change Data Capture (CDC) streaming via Debezium and Apache Kafka.

## Running the app
To run the app, simply clone and run the following:
```
docker compose up --build -d
```